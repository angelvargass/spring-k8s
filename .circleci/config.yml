version: 2.1

jobs:
  run-tests:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          mvn test

workflows:
  say-hello-workflow:
    jobs:
      - run-tests:
          filters:
            branches:
              only:
                - dev
                - qa

version: 2.1

jobs:
  run-tests:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run: mvn dependency:go-offline
      - run: ./mvnw test

workflows:
  run-tests-workflow:
    jobs:
      - run-tests:
          filters:
            branches:
              only:
                - dev
                - qa